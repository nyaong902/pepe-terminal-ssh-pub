# v2.2.1

## 오피스 · PDF · FlowChart · 미디어 플레이어 워크스페이스 (신규)

- **오피스 워크스페이스**: 형식 선택 화면에서 한글(HWP/HWPX, rhwp 기반) · 워드/엑셀/파워포인트(ZIZIYI Office) · PDF · FlowChart(draw.io) 중 골라 새 탭으로 염. 형식별 최근 문서 목록, 검색, 다중 문서 미니탭 지원
- **PDF 편집**: pdf.js 기반. 한글/HWP에서 내보낸 PDF처럼 폰트가 임베드되지 않고 CID로만 참조되는 문서는 표/테두리는 보이는데 글자가 아예 안 보이던 문제를 CMap/표준 폰트 데이터 번들로 수정
- **미디어 플레이어 워크스페이스**: WAV/A-law/u-law(로컬 디코딩) 및 EVS/AMR-NB/AMR-WB/OPUS(GStreamer 사이드카) 재생, `#!ENC` 암호화 파일 비밀번호 해제, 최근 재생 목록, Audacity 스타일 파형 편집기(선택/자르기/삭제/페이드/게인/내보내기)
- **영상 재생 추가**: mp4/m4v/mov/webm/ogv를 Chromium 내장 디코더로 재생(별도 사이드카 불필요). 초기엔 `file://` 방식으로 검은 화면만 뜨던 문제를, PDF/오피스와 동일하게 IPC로 바이트를 읽어 Blob URL로 재생하는 방식으로 수정

## 설치 시 기능 선택 (신규)

- 설치 프로그램에 VPN(OpenVPN, ~9MB) · MicroSIP(~104MB) · SIPp(~15MB) · 미디어 코덱(GStreamer, ~49MB) · **오피스**(한글/워드/엑셀/파워포인트/FlowChart 편집기, ~220MB) 체크박스 추가 — 필요 없는 기능은 설치 용량에서 제외 가능
- 오피스/미디어 관련 3개 대용량 번들(office-editor/rhwp-studio/flowchart-editor)을 앱 패키지 안(app.asar)이 아닌 별도 리소스 폴더로 분리해, 설치 후에도 선택적으로 삭제 가능하게 구조 변경
- 설치 시 제외한 기능은 커맨드 팔레트·메뉴 > 도구·"+" 워크스페이스 메뉴에서 해당 항목이 자동으로 숨겨짐(실제 파일 존재 여부를 실시간 확인 — 나중에 폴더를 수동으로 넣거나 빼도 항상 일치)
- 선택 해제한 번들 삭제 속도 개선: 파일 수천 개짜리 폴더(FlowChart 라이브러리 등)에서 한 파일씩 로그를 찍던 기본 삭제 방식 대신, 한 번에 통째로 지우는 방식으로 교체해 설치 시간 단축
- 자동 업데이트 실행 시에는 이 선택 페이지를 건너뛰고, 이전 설치에서 고른 값을 레지스트리에서 그대로 재적용

## EVS 코덱

- 자체 제작한 GStreamer용 EVS 코덱 플러그인 어댑터 소스와 크로스플랫폼 빌드 가이드 추가(라이선스 문제로 실제 코덱 바이너리 자체는 공개 저장소에 포함하지 않음)
- EVS 디코더 진폭 버그 수정 — 패킹 안 된 프레임 데이터를 그대로 넘겨 소리가 원래 볼륨의 1/1000 수준으로만 재생되던 문제
- GStreamer 레지스트리 캐시 손상 문제 수정 — 첫 재생 이후 두 번째 EVS 파일부터 재생이 깨지던 문제(실행마다 새 레지스트리 사용)
- 공개 저장소로 클론해 빌드하면 더미 플레이스홀더가 로드되므로, EVS 파일 재생 시 알아보기 쉬운 안내 메시지를 표시(원인 불명의 플러그인 로드 실패 대신)

## SIPp 워크스페이스 확장

- REGISTER/OPTIONS/INFO/REFER/UPDATE/PRACK/SUBSCRIBE/NOTIFY/MESSAGE/PUBLISH 등 SIP 메서드 추가(INVITE/ACK/BYE/CANCEL 외)
- `-inf` 데이터 파일 카드 위치 조정, 신규 전송 블록의 From/To를 주입 필드값으로 자동 채움
- 저장된 시나리오에서 데이터 파일 내용이 누락되던 저장 버그 수정
- 시나리오/통계 화면이 순간적으로 비어 보이며 깜빡이던 문제 수정

## 터미널 검색

- 별도 OS 창으로 분리되던 "검색창 떼어내기"를 제거하고, 고정/자유 이동 토글 방식의 인라인 검색창으로 교체 — 위치·상태 동기화 문제 근본 해결
- 하이라이트 표시를 직접 그리던 방식에서 xterm-addon-search의 네이티브 decorations로 교체 — 스크롤/리사이즈/미니탭 전환 시 하이라이트가 엉뚱한 줄에 붙거나 사라지던 문제들을 근본적으로 해결
- 검색 기록을 파일로 영구 저장(기존엔 새로고침마다 초기화)
- 글꼴 크기 등 숫자 입력란이 타이핑 중간에 값이 잘려나가던 문제 수정

## 그 외 버그 수정

- 세션을 복사/복제뿐 아니라 그냥 새로 추가할 때도 같은 이름으로 여러 개 생기던 문제 수정
- 커스텀 워크스페이스 터미널 슬롯에서 세션 연결 시 저장된 테마/글꼴이 적용되지 않던 문제, 우클릭 메뉴가 바깥 클릭/Esc로 안 닫히던 문제 수정
- "+" 워크스페이스 메뉴 및 메뉴 > 도구의 라벨에서 불필요한 "워크스페이스" 표현 정리, X 서버 시작/중지/상태 툴바 버튼 3개를 아이콘 하나 + 드롭다운 메뉴로 통합
- 자동 업데이트 실행 시 "사용자 데이터 삭제" 확인 창이 뜨며 화면 없이 멈춘 것처럼 보이던 문제 수정

---

## 산출물
- `PePe-Terminal-SSH-Setup-2.2.1.exe` (NSIS 설치본 — 기능 선택 설치 지원)
- `PePe-Terminal-SSH-2.2.1-portable.exe` (포터블)
