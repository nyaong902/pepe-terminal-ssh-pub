# v2.4.2

**로그 분석 워크스페이스에 pcap 뷰어가 들어왔습니다.** Wireshark 를 따로 켜지 않고 패킷 목록·콜플로우·
필드 트리·hex 를 앱 안에서 봅니다. 파일은 창에 끌어다 놓으면 알맞은 워크스페이스에서 열리고,
HIWARE 같은 자산관리툴에서 접속하면 창이 쌓이지 않고 **탭**으로 붙습니다.
터미널 단어 찾기는 XShell 처럼 읽히도록 바뀌었고, 로그가 폭주하는 중에도 하이라이트가 빠지지 않습니다.

## 🔍 pcap 뷰어 — Wireshark 방식으로

`.pcap` / `.pcapng` 파일을 로드하면 패킷 목록이 뜹니다. 행을 고르면 아래에 **필드 트리**와
**hex/ASCII 덤프**가 나옵니다.

```
No  Time                        Protocol  Endpoint                       Length  Info
 8  2026-07-24 11:24:32.707007  SIP       172.16.45.18:5060 → …:61698      629  BYE sip:070…
```

- **hex ↔ 트리 양방향 연동** — hex 의 바이트에 마우스를 올리거나 클릭하면 그 바이트를 가진
  **가장 안쪽 필드**를 트리에서 짚어 줍니다. 접혀 있던 상위 계층은 자동으로 펼쳐지고 그 줄로
  스크롤합니다. 반대로 트리에서 필드를 클릭하면 hex 가 그 바이트 줄로 갑니다
- **관련 패킷 세로선** — 패킷을 고르면 관련된 패킷을 왼쪽 여백에서 한 줄로 이어 줍니다.
  SIP 는 **같은 통화(Call-ID)**, 그 외에는 **같은 대화(양방향 IP:포트)** 기준입니다.
  사이에 무관한 패킷이 있어도 선은 관통합니다
- **헤더 클릭 정렬** — 누를 때마다 오름차순 → 내림차순 → 원래 순서로 돕니다.
  Time 은 숫자로 비교하므로 `10.5` 가 `9.1` 뒤로 갑니다. 정렬해도 고른 행은 따라갑니다
- **시간 표시 형식 10가지** — 날짜와 시간(기본) · 년/연중일 · 시간 · epoch 초 ·
  최초 캡처부터 · 이전 캡처부터 · 이전 표시부터 · UTC 3종. 콜플로우의 시간도 함께 바뀝니다
- **콜플로우 함께 보기** — 표와 시퀀스 다이어그램을 나란히 봅니다. 화살표를 누르면 표가 그
  패킷으로 이동하고, 표에서 고르면 화살표가 강조됩니다
- **좌우 폭 조절** — 트리/hex 사이, 표/콜플로우 사이를 끌어서 나눕니다

**아래쪽이 잘려 보이던 문제**도 고쳤습니다. 창 높이를 재둔 값이 한 번 어긋나면 그대로 굳어서,
그 값으로 계산한 높이 합이 실제 영역을 넘고 넘친 부분(약 23px)이 잘려 마지막 줄이 반만 보였습니다.
이제 비율로 나눠 브라우저가 매번 다시 계산하므로 넘치는 것이 불가능합니다.

## 🖱 파일을 끌어다 놓으면 알맞은 곳에서 열립니다

앱 **상단 타이틀바의 빈 자리**에 파일을 끌어다 놓으면 확장자를 보고 알아서 엽니다.

| 놓은 파일 | 열리는 곳 |
|---|---|
| 음성·영상 | 미디어 워크스페이스 |
| 워드·엑셀·한글·PDF | 오피스 워크스페이스 |
| `.pcap` / `.pcapng` | **미디어(RTP 재생) / 로그 분석 중 선택** |
| 그 밖의 파일 | 파일 에디터 |

- 구글 시트 같은 **웹 주소를 오피스 파일로 오인**해 열려다 실패하던 것을 걸러냅니다
- pcap 스트림을 재생할 때 남던 임시 폴더를 문서를 닫거나 워크스페이스를 떠날 때 정리합니다
  (비정상 종료로 남은 것은 다음 실행 때 지웁니다)

## 🔗 외부 툴에서 접속하면 창이 아니라 탭으로

HIWARE 등 자산관리툴은 접속할 때마다 터미널 프로그램을 **새로 실행**합니다. 그래서 접속을
반복하면 PePe 창이 계속 쌓였습니다. 이제 Xshell 처럼 **이미 열려 있는 창에 새 탭**으로 붙습니다.

터미널 설정의 파라미터는 아래 형식을 모두 이해합니다 — 지금 쓰시는 값을 그대로 두면 됩니다.

```
-telnet <IP> <PORT>                                   (PuTTY 형식)
<TITLE_CHANGE_OFF> /url telnet://<IP>:<PORT> -newtab  (Xshell 형식)
-ssh <IP> -P <PORT>                                   /T=1 <IP>:<PORT>
```

> **아이콘으로 그냥 실행한 것은 예전처럼 새 창이 뜹니다.** 창을 여러 개 띄우는 사용법은
> 그대로입니다 — 접속 정보를 넘겨받은 경우에만 탭으로 붙습니다.

## 🖥 터미널 — 창 크기를 바꿀 때 스크롤이 밀리던 문제

맨 아래를 보고 있다가 **창을 최대화하면 화면이 위로 올라가** 있었고, 복원해도 제자리로 돌아오지
않았습니다. 크기가 바뀌면 터미널이 내용을 다시 배치하면서 보던 위치를 잃어버리는데, 탭 전환
경로에는 그것을 되돌리는 처리가 있었지만 **창 크기 변경 경로에는 없었습니다.**

이제 맨 아래를 보고 있었으면 계속 맨 아래에, 스크롤을 올려두었으면 그 줄에 머무릅니다.

## ⚡ 터미널 렌더러를 고를 수 있습니다 (옵션 > 터미널 > 렌더러)

로그가 쏟아질 때 CPU 가 튀는 것이 신경 쓰이면 **WebGL 렌더러**를 켜 보세요. 화면을 CPU 대신
GPU 로 그립니다.

```
□ WebGL 렌더러 사용 (실험적)
```

| | 기본(꺼짐) | 켜면 |
|---|---|---|
| 창 투명도 | 창 뒤가 보입니다 | **불투명해집니다** |
| 로그 폭주 중 CPU | 9~10% 로 튀는 구간이 있습니다 | 안정적입니다 |
| GPU | 거의 안 씁니다 | 10% 정도 올라갑니다 |

**기본은 꺼짐**입니다 — 창 투명도를 지키는 쪽을 기본으로 두었습니다. 둘 다 장단이 있어 환경에
따라 다를 수 있으니 직접 비교해 보시고, 켜고 끄면 **이미 열려 있는 터미널에도 바로 적용**됩니다
(재기동 필요 없음).

## 🔎 터미널 단어 찾기 — XShell 처럼

찾은 단어가 **읽히지 않았습니다.** 하이라이트 색이 글자를 덮고, 현재 매치에는 선택 박스가 겹쳐
있었습니다.

```
일반 매치  노란 배경 + 세션 배경색 글자
현재 매치  세션 글자색 배경 + 세션 배경색 글자
```

- 현재 매치를 덮던 **선택 박스를 없앴습니다**. 다음 찾기를 할 때마다 단어가 **클립보드에 복사되던
  것**도 사라졌습니다
- 스크롤하거나 창 크기를 바꿔도 **하이라이트가 빠지지 않습니다.** 검색 중에 다른 세션 탭이나
  미니탭으로 옮기면 그 터미널에는 하이라이트를 그려주는 연결이 걸리지 않아, 이전/다음 찾기를
  누를 때만 나타났습니다
- **이전/다음 찾기가 엉뚱한 곳으로 뛰지 않습니다.** 화면이 맨 아래에 있으면 그 근처부터 찾습니다
  (예전에는 내부 목록 순서를 따라 버퍼 한참 위로 뛰었습니다)
- 잔상·글자가 떠 보이던 증상 해결
- **로그 폭주 중 검색 비용을 크게 줄였습니다** — 화면과 내용이 그대로면 다시 훑지 않고(약 57% 절감),
  읽는 글자 수를 45% 줄이고, 매 화면마다 한 번씩 강제로 다시 그리던 것을 없앴습니다.
  실측 CPU 0.55%, 메모리 누수 없음

## 💬 AI 챗

- **스크롤바가 창 밖으로 나가고 화면이 떨리던 문제** — 대화 이력을 바꿀 때마다 흔들렸습니다.
  다이어그램을 그리는 임시 영역이 페이지 폭을 늘렸다 줄이던 것이 원인이었습니다
- **mermaid 다이어그램이 코드로 보이던 문제**
- **메신저에 갔다가 돌아오면 다이어그램이 코드로 남던 문제** — 화면을 전환하면 대화 영역이
  새로 만들어지는데, 그때 다시 그리는 처리가 돌지 않았습니다
- **다이어그램으로 바뀌기 전 원본 코드가 잠깐 보이던 것**도 없앴습니다 — 한 번 그린 그림은
  기억해 두고 다시 쓰며(다시 계산하지 않습니다), 바뀌기 전 코드는 화면에 나가지 않습니다
- 다이어그램이 여러 개인 긴 대화의 반응 속도 개선(화면 밖 메시지는 그리지 않습니다)

## 💬 메신저

- **대화를 지우면 그 대화의 사진·파일도 함께 지웁니다.** 보관 기간이 지나 자동으로 지워질 때도
  같습니다. 예전에는 대화만 사라지고 받은 파일이 계속 쌓였습니다

## 🔧 그 밖에

- **원격 공유에서 Ctrl/Alt 조합키**가 상대 터미널로 전달되지 않던 문제
- 세션/파일 트리 **사이드바 폭 계산**, 세션 이름을 다시 눌러 바꿀 때의 동작 개선
- 개발자 도구 콘솔에 계속 쌓이던 진단 로그(단어 찾기 이동, 대화 아카이브 진행)를 기본으로
  끕니다 — 문제 확인이 필요할 때만 켭니다

---

## 산출물
- `PePe-Terminal-SSH-Setup-2.4.2.exe` (NSIS 설치본)
- `PePe-Terminal-SSH-2.4.2-portable.exe` (포터블)
