# v2.2.7

## 자동 업데이트 — SmartScreen 우회 시도

v2.2.6에서 확보한 진단 로그로, 문제가 재현되는 PC와 정상 동작하는 PC의 로그가 `elevate.exe` 실행 시점까지 완전히 동일하다는 게 확인됐습니다. 그 PC에서는 UAC 승인까지는 뜨는데, 승인 후 설치 창이 조용히 안 뜹니다 — 이 지점은 electron-updater/우리 로그로는 볼 수 없는 영역입니다.

가장 유력한 원인은 Windows SmartScreen입니다. 이 앱은 신뢰된 CA가 아닌 자체 서명 인증서를 쓰는데, GitHub에서 막 다운로드한 실행 파일에는 "인터넷에서 받음" 표시(NTFS Zone.Identifier)가 붙어 SmartScreen 평판 검사를 유발합니다. 개발자 PC는 이 발행자를 이미 여러 번 실행해봐서 평판이 쌓여 있지만, 처음 보는 PC에서는 이 검사가 다르게(우회 대화상자도 없이 조용히) 동작할 수 있습니다.

- "재시작하여 설치"를 누르는 시점, 다운로드된 설치 파일에서 이 "인터넷에서 받음" 표시를 미리 제거하도록 추가 — 자동 업데이트 앱들이 흔히 쓰는 안전한 방법(`Unblock-File`과 동일한 효과)

**계속 안 되면**: v2.2.6과 마찬가지로 문제가 재현되는 PC에 이 버전을 먼저 수동 설치해둔 뒤, 다음 버전으로 자동 업데이트를 시도했을 때 `update-debug.log`의 `Zone.Identifier` 관련 줄을 확인해주세요 — 표시 자체가 없었다면(`Zone.Identifier 없음` 로그) 이 가설도 아니라는 뜻이니 다른 원인을 찾아야 합니다.

---

## 산출물
- `PePe-Terminal-SSH-Setup-2.2.7.exe` (NSIS 설치본)
- `PePe-Terminal-SSH-2.2.7-portable.exe` (포터블)
