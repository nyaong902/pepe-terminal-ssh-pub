# v2.2.19

## 사용자별 설치로 전환 (관리자 권한 불필요)

지금까지 여러 PC에서 자동 업데이트 설치 창이 안 뜨던 문제(v2.2.1~v2.2.18에 걸쳐 elevate.exe 직접 실행, 지연 종료, Job Object 분리, 창 강제 표시 등 다양한 시도)는 결국 "관리자 권한 상승(UAC)"이 전제인 전체 사용자용 설치 방식 자체가 근본 원인으로 보입니다. 검증된 다른 Electron 앱(Tabby)을 참고해, 이 앱도 **사용자별 설치**로 전환합니다.

- 설치 위치: `Program Files` → `%LocalAppData%\Programs\PePe Terminal(SSH)`
- 설치/업데이트에 관리자 권한이 더 이상 필요 없음 — UAC 창, elevate.exe, Job Object 관련 문제 전부 원천적으로 사라짐
- 기능 선택 값 저장 위치를 레지스트리 `HKLM`에서 `HKCU`로 변경(관리자 권한 없이도 쓸 수 있도록)
- 관리자 권한이 없어져서 자동 업데이트 중에도 "사용자 데이터 삭제 확인" / "기능 선택" 커스텀 페이지를 다시 안정적으로 보여줄 수 있게 됨

**참고**: 한 PC를 여러 사용자가 같이 쓴다면 각자 따로 설치해야 합니다. 기존에 `Program Files`에 설치돼 있던 사용자는 이번 업데이트로 새 위치에 설치되며, 필요하면 기존 설치를 제어판에서 별도로 제거해주세요.

---

## 산출물
- `PePe-Terminal-SSH-Setup-2.2.19.exe` (NSIS 설치본)
- `PePe-Terminal-SSH-2.2.19-portable.exe` (포터블)
