# v2.2.0

## SIPp 워크스페이스 (신규)

- 네이티브 SIPp(부하 발생기) 를 앱 안에서 직접 spawn/제어 — sidecar 프로세스, IPC 로 시작/중지/통계 스트림
- 블록 조립형 시나리오 빌더 — 전송(INVITE/ACK/BYE/CANCEL/커스텀)/응답 대기/일시정지 블록을 쌓아서 통화 흐름 구성
- 블록 ↔ 생성된 시나리오 XML 미리보기 양방향 동기화 (XML 직접 편집 시 블록 목록으로 역파싱)
- 실행 중 실시간 CPS(초당 콜 수) 조절 — SIPp CLI 확장(`-ctrl_file`) 으로 실행 파일 자체에 패치
- CSV `-inf` 주입 파일 지원, 시나리오 저장/불러오기 (이름 붙여 관리)
- 개별 라벨이 붙은 CLI 옵션 필드, 탭별 프로세스 격리
- 시나리오 블록 순서 변경 — 블록 아무 곳이나 드래그, 또는 블록 클릭 후 `Ctrl+↑/↓`

## 브라우저 워크스페이스

- 자격증명 자동완성 추가
- **레이아웃 붕괴 버그 수정**: `forceBrowserLightTheme()` 가 모든 페이지에 주입하던 `body { height: auto !important }` 가 원인 — `height:100%` 로 부모에 맞춰 늘어나야 하는 사이트의 내부 flex 래퍼(footer 하단 고정 등)가 퍼센트 높이 기준을 잃어 콘텐츠가 위로 쏠리고 아래 여백이 남던 문제. 이전 세션의 커스텀 워크스페이스 "150px 고정" 버그 우회책이 남긴 부작용이었음.
- 일반 브라우저 탭의 `<webview>` 리사이즈 로직에서 `autosize` 제거(커스텀 워크스페이스 슬롯과 동일한 CSS-only 방식으로 통일), 실제로는 아무 효과 없던 `resizeBrowserGuest` IPC 호출(`WebContents` 에 존재하지 않는 `setSize` 를 부르고 있었음) 제거
- 반복적으로 뜨던 `MaxListenersExceededWarning`(`did-stop-loading`) 콘솔 경고 수정 — 게스트 `WebContents` 생성 시점에 동기적으로 `setMaxListeners` 상향

## 커스텀 워크스페이스 & 터미널 프로세스 분리

- 브라우저·세션 비교·로그 분석·MicroSIP 패널을 그리드로 자유 배치하는 커스텀 워크스페이스 레이아웃
- 터미널/패널 단위 프로세스 분리(`WebContentsView`) — 한 탭의 무거운 SSH 출력이 다른 탭 반응성에 영향을 주지 않음. SSH 워커스레드 마이그레이션 동반
- 특수 워크스페이스 좌우 분할(1차/2차), 파일트리·세션 사이드바 통합
- 좌우 분할 비율 드래그 시 렉 제거, 드래그 중 webview 가 마우스 이벤트를 가로채던 문제 수정
- 재도킹된 워크스페이스 탭이 활성 탭에 잘못 병합되던 문제, 탭이 하나만 남았을 때 새 창으로 분리되던 문제 수정

## 앱 전역 UI

- **스크롤바**: Windows 터미널 스타일로 통일 — 평소엔 얇게, 스크롤바 픽셀 영역에 마우스가 있을 때만 두껍게 확장(화살표 버튼 포함). CSS `:hover` 만으로는 스크롤바 영역만 정확히 구분할 수 없어 마우스 좌표를 직접 추적하는 방식(`src/utils/scrollbarHover.ts`)으로 구현
- 작업일지(work journal) — 캘린더 뷰 + AI 자동 기록
- 스티커 노트 — 사이드바로 최소화되는 매니저 패널(검색/생성/삭제)
- 전역 커맨드 팔레트(`Ctrl+W`)
- 파일 탐색기 임의 행/열 분할
- 메신저 이미지 첨부/드래그앤드롭/이모티콘/메시지 회수
- 설치 프로그램에 제거 시 사용자 데이터 삭제 여부를 고르는 체크박스 페이지 추가

## 성능 개선

- 다중 세션에서 대량 출력 발생 시 버벅임·메모리 사용량 개선
- 포커스 손실 및 다중 탭 사용 시 버벅임 근본 원인 수정
- 개발 모드 메모리 최적화

## 그 외 버그 수정

- SSH 워커스레드 RPC 경계에서 발생하던 Buffer 손상 수정
- MCP `ssh_exec` 가 오류 시 무한 대기하던 문제 수정
- `Ctrl+S` 를 누르면 터미널이 멈추던 문제 수정 (XOFF 처리)
- DevTools 접근, 암호화 방식 협상 재시도 폭주, 파일 전송 오류 스팸, 키보드로 세션 열 때 포커스가 새던 문제 수정
- 워크스페이스 옵션 다이얼로그 취소 시 이전 상태 복원, 클로드 채팅 에이전트 전환 시 레이아웃 흔들림 수정

---

## 산출물
- `PePe-Terminal-SSH-Setup-2.2.0.exe` (NSIS 설치본)
- `PePe-Terminal-SSH-2.2.0-portable.exe` (포터블)
