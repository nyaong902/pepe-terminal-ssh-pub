# PePe Terminal(SSH) v2.1.8

> 베이스: v2.1.7 + **멀티 윈도우(탭/미니탭 분리·재부착) · 외부 프로그램 SSH 자동 접속 · TELNET 접속 모드 · 자동 업데이트 안정화 · 다수 버그 수정**

## 🪟 멀티 윈도우 — 탭/미니탭 분리·재부착

- **탭 또는 미니탭(세션)을 창 밖으로 드래그**하면 새 창으로 분리됩니다.
- **다른 PePe 창 위에 드롭**하면 그 창으로 다시 합쳐집니다(re-dock).
  - 패널 **가장자리(상/하/좌/우)** 드롭 → 그 방향으로 **분할**
  - 패널 **중앙** 드롭 → **미니탭으로 병합**
- 탭/미니탭 **우클릭 → "새 창으로 열기"** 메뉴로도 분리 가능.
- **라이브 세션 유지**: 분리/재부착 시 백엔드 SSH 세션을 끊지 않고 동일 termId로 재부착합니다. 터미널 데이터를 모든 창에 broadcast 하고 연결 상태를 시딩해 입력/출력이 그대로 이어집니다.
- **화면·테마 이관**: 분리 시점의 화면 버퍼(스크롤백 일부 포함)와 테마/폰트/커서/스크롤백 설정을 새 창으로 옮겨, 분리해도 동일한 모습으로 보입니다. (`xterm-addon-serialize` 도입)
- 분리 창의 타이틀바(최소화/최대화/닫기/이동)는 자기 창을 제어하도록 `window:*` 핸들러를 요청 창 기준으로 변경. 마지막 세션을 재부착해 비게 된 분리 창은 자동으로 닫힙니다.

## 🔗 외부 프로그램 연동 / TELNET

- **외부 자산관리·접근통제(PAM) 툴**에서 PuTTY/Xshell/SecureCRT/TeraTerm 자리에 PePe를 등록해 SSH 자동 접속. `ssh://`, `telnet://`, PuTTY `-ssh/-telnet <IP> [<PORT>]`, TeraTerm `/T=1 <IP>:<PORT>`, `host:port` 등 다양한 인자 형식 파싱.
- **TELNET(raw TCP) 접속 모드** — 게이트웨이가 `127.0.0.1:<port>`로 평문(TELNET) 스트림을 내주는 접근통제 솔루션용. IAC 협상(TTYPE/NAWS/SGA/ECHO) 처리, 인코딩 지원.

## 🔄 자동 업데이트

- 시작 시 자동 확인 + 수동 확인 버튼 (GitHub Releases).
- 자체 서명 인증서 신뢰 체인 문제 우회(서명 검증 스킵 + publisherName strip), 다운로드 진행 표시에 버전명 노출.

## 🐛 버그 수정 / 개선

- **콘솔 창 회귀 수정**: Codex(`codex.exe` 직접 spawn) 작업 시 콘솔 창이 포그라운드를 가로채 메인 창이 사라지던 문제 — Codex/Gemini/Claude 모든 spawn에 `windowsHide` 적용.
- **Codex 전체 권한(full-auto)**: 설정 시 계획/승인 단계 없이 즉시 실행.
- **파일 전송**:
  - 세션의 **현재 작업 경로(cwd)**로 파일 전송 열기 (자동추적 OFF 세션에서 stale 홈 경로로 가던 문제 수정).
  - 세션 우클릭/빠른연결 SFTP는 **단일 파일 전송 워크스페이스에 새 탭**으로 누적 (파일 전송 버튼만 새 워크스페이스 생성).
  - 특수 워크스페이스(파일 전송 등)에 세션 연결 시 안내 후 새 워크스페이스로.
- **AI Chat**: 첨부 버튼 `📎 +`, 입력창에 드래그앤드롭 안내, **중단 버튼이 요청 시작과 동시에 활성화**. 메시지 내 http(s) 링크 클릭 시 앱 창을 덮지 않고 **기본 브라우저로** 열기.
- **터미널 마우스 설정** — 오른쪽/가운데 단추 동작 선택(팝업 메뉴 / 클립보드 붙여넣기 / 등록 정보 / 캐리지 리턴 / 선택 텍스트 붙여넣기 / 없음).
- **레거시 SSH 서버 호환**(diffie-hellman-group14-sha1 등) 및 MCP SSH 서버 EPIPE 방어.
- **다중 인스턴스** 동시 실행 시 캐시 충돌 방지.

## 📦 빌드

- 버전: 2.1.8
- 다운로드: `release/PePe-Terminal-SSH-Setup-2.1.8.exe` (NSIS installer, 서명됨) / `release/PePe-Terminal-SSH-2.1.8-portable.exe` (portable, 서명됨)
- 자동 업데이트: GitHub `v2.1.8` 릴리즈에 `latest.yml` + Setup exe + `.blockmap` 업로드.
  - ⚠ 자체 서명 우회가 없는 구버전(v2.1.5/v2.1.6 등)에서는 자동 업데이트가 막힐 수 있어, 한 번은 수동 설치가 필요할 수 있습니다.

---

### 포함 커밋 (v2.1.7 → v2.1.8)

- `3ce31be` 외부 프로그램에서 PePe로 SSH 자동 접속 + v2.1.8
- `5919ddb` 네이티브 재빌드 유발 devDep 제거 + 서명 타임스탬프 서버 로테이션
- `25ef02d` 접근통제 솔루션 로컬 평문 프록시용 TELNET(raw TCP) 접속 모드
- `de1a1d8` codex.exe spawn windowsHide — 콘솔 창 포커스 탈취로 메인 창 사라지는 문제
- `d656bb0` gemini/claude shell spawn windowsHide
- `c31d2b9` codex 전체 권한(full-auto) 즉시 실행
- `ff927bf` 특수 워크스페이스 세션 연결 시 안내 후 새 워크스페이스
- `4335fad` 터미널 마우스 단추 동작 설정
- `3eb360b` 파일 전송 세션 현재 경로(cwd)로 열기
- `82ee691` AI Chat 첨부 버튼 📎+ · 드래그앤드롭 안내 · 중단 버튼 즉시 활성화
- `e86911b` 멀티 윈도우 탭/미니탭 분리·재부착 + 파일전송 탭 재사용 + 외부링크 외부 브라우저
