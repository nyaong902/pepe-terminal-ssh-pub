# v2.4.4

**터미널이 출력을 버리지 않습니다.** 이번 판의 핵심입니다 — 명령 결과의 가운데가 조용히
사라지던 문제를 뿌리부터 걷어냈습니다. 그리고 한/영 상태를 터미널·AI 챗·메신저가 각자
기억하고, 포스트잇에 서식과 색상이 붙었습니다.

## 🔴 SSH 출력이 사라지던 문제 — 걷어냈습니다

`desc` 한 방의 결과에서 **가운데 12줄이 없어지고 앞뒤가 한 줄에 붙어** 있었습니다.

```
D_PNP_USER_GRPID   BIGINT   FIXED       D_SVCBITS7   BIGINT   FIXED
                                        ↑ 여기서 앞뒤가 이어붙었다
```

폭주 판정이 *"이 순간(8ms) 6KB 를 넘었나"* 였습니다. 링크가 빠르면 8ms 안에 7KB 쯤은 예사로
옵니다 — 폭주가 아니라 그냥 명령 하나의 출력입니다. 전체가 11.5KB 뿐인데도 폭주로 오판해서
앞부분을 버렸습니다.

판정을 "최근 1초쯤 계속 많이 왔는가"로 고쳤지만, 그것만으로는 `cat` 으로 큰 파일을 뿌릴 때
여전히 가운데가 사라졌습니다. **그래서 버리기 자체를 없앴습니다.** 화면에 없는 줄은 되돌릴
방법이 없고, 어떤 CPU 이득으로도 정당화되지 않습니다.

- 실측: `desc` 11.5KB — 예전엔 858B 손실 → 이제 0B. `cat` 12MB — 전량 그대로.
- 한글이 청크 경계에 걸려 쪼개져도 깨지지 않습니다.
- **폭주 중 CPU 가 튄다면 옵션 > 터미널 > 렌더러(WebGL) 를 켜세요.** 원래 이 문제의 원인은
  데이터량이 아니라 렌더 비용이었습니다 — 같은 로그를 Xshell 은 전부 보여주면서 CPU 3.5%
  였습니다.

## 🇰🇷 한/영 상태를 각자 기억합니다

Windows 의 한/영 은 **창 단위**입니다. 그래서 AI 챗에서 한글로 쓰다 터미널로 돌아오면 한글
그대로였고, 그 반대도 마찬가지였습니다.

이제 **터미널 · AI 챗 · 메신저**가 각각 자기가 쓰던 상태를 기억하고 돌아올 때 되돌립니다.
터미널은 영문으로 시작하고, 챗·메신저는 한 번 쓴 뒤부터 기억합니다.

판단은 키 입력으로 합니다 — IME 가 키를 삼켰으면 한글, 알파벳이 그대로 왔으면 영문. 검색창
같은 다른 입력란을 중간에 거쳐도 값이 섞이지 않습니다.

## 🖥 터미널

- **vi 에서 위로 스크롤한 "이전 화면"** — 단어 구분기호가 안 먹고 드래그 복사도 안 됐습니다.
  이 화면은 원본과 별개의 터미널이라 설정과 복사 경로가 닿지 않았습니다. 이제 더블클릭 단어
  선택과 `Ctrl+Shift+C`·우클릭 복사·텍스트 편집기로 보내기가 모두 동작합니다
- **스크롤바 위에서 휠** — 가로 막대는 가로로, 세로 막대는 세로로 스크롤합니다.
  파일 비교(Monaco)와 SSW CDR 로그 분석에서도 동작합니다

## 🗂 워크스페이스 — 우측에 함께 보기

오피스 · 미디어 · SSW CDR 로그 분석 · 대화 아카이브 검색 · Pepe-Box 에서 **탭 우클릭 메뉴에
"우측에 함께 보기" 가 아예 뜨지 않았습니다.** 다섯 타입이 분할 대상 목록에서 빠져 있었습니다.

Pepe-Thing 은 목록에 있었는데도 안 됐는데, 원인은 반대쪽이었습니다 — 활성 탭이 위 다섯 중
하나면 우측 탭이 적격이어도 아무것도 그려지지 않았습니다.

## 📝 포스트잇

Windows 스티커 메모처럼 손봤습니다.

- **서식 도구** — 굵게 · 기울임꼴 · 밑줄 · 취소선 · 글머리 기호 · 그림 넣기.
  켜진 서식은 눌린 모양으로 보입니다
- **도구는 마우스를 올렸거나 작성 중일 때만** 보입니다. 자리는 그대로 둬서 나타날 때 본문이
  밀리지 않고, 제목 바를 잡아 옮기는 것은 도구가 숨어도 됩니다
- **색상 7가지** — `⋯` 메뉴에서 고릅니다. 메인 창의 스티커 메모 목록도 같은 색으로 보입니다
  (노랑 고정이었습니다). 같은 메뉴에 노트 목록 · 메모 삭제
- 본문 자리표시("메모를 작성하세요...")

> 기울임꼴이 아무 효과도 없던 것은 서식 명령이 아니라 글꼴 설정 때문이었습니다. 앱 전역의
> `font-synthesis: none` 이 합성 이탤릭을 막아서, 이탤릭 자체(字體)가 없는 한글 글꼴에서는
> 변화가 없었습니다. 메모 본문에서만 합성을 되살렸습니다.

## 💬 메신저

- **새 메시지를 카카오톡 "나와의 채팅" 으로 알림** — 설정에서 카카오 로그인 후 켜면 됩니다
- **설정을 펼치면 사용자 목록 자리가 뒤집혀 설정이 나옵니다** — 예전엔 목록 위에 설정이
  쌓여서 사용자가 한 줄만 보였습니다. 이제 양쪽이 사이드바 전체를 씁니다
- 카카오 연동 설정은 접힌 상태로 시작하고, 설정 맨 아래에도 접기 버튼이 있습니다
- **상대 PePe 버전 표시가 2.4.1 ↔ 2.4.3 로 계속 바뀌던 문제**

## 🤖 AI 챗

- **Claude+ 에이전트** — 세션당 프로세스를 계속 살려두는 방식이라 **응답 생성 중에도 새 지시를
  끼워 넣을 수 있습니다.** 옵션 > AI 의 "Claude 대신 Claude+ 사용" 을 켜면 에이전트 선택창의
  Claude 자리가 Claude+ 로 동작합니다
- **세션 이름을 바꿨는데 SSH 컨텍스트 선택에 옛 이름이 남던 문제**

## 🔍 파일 비교

- 비교 목록 행 우클릭에 **재검사 · ctcocr · ctcicr** 추가(CR 번호는 세션 내 기억)
- **개행(EOL)과 인코딩 차이를 구분해 안내**하고 각각 통일·변환 저장 가능
- 인코딩 배지를 눌러 인코딩을 고르면 다시 디코딩하고, 저장할 때 그 인코딩으로 씁니다
- SFTP 저장을 임시파일 + 이름 바꾸기로 바꿔 **ClearCase 뷰 파일이 잘리던 충돌을 피합니다**

---

## 산출물
- `PePe-Terminal-SSH-Setup-2.4.4.exe` (NSIS 설치본)
- `PePe-Terminal-SSH-2.4.4-portable.exe` (포터블)
